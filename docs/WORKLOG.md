# 작업 기록 (WORKLOG)

> **이 파일은 사람과 여러 AI 도구(Claude Code, Codex 등)가 같이 보는 로그입니다.**
> 코드를 바꿨으면 여기에 한 줄 남겨주세요. 다음에 붙는 사람이 "왜 이렇게 되어 있지?"를
> 커밋 로그와 이 파일만 보고 따라올 수 있어야 합니다.

---

## 0. 작성 규칙

- **시간은 KST(UTC+9)** 로 적습니다. 컨테이너는 UTC라서 `date -u` 결과에 9시간을 더하면 됩니다.
- 형식: `## YYYY-MM-DD` 아래에 `### HH:MM · 한 줄 제목 · [도구]` 를 시간순으로 쌓습니다.
- 한 항목에 **무엇을 / 왜 / 어디를** 세 가지를 적습니다. 특히 **왜**를 빼먹지 마세요.
  시뮬레이션이나 측정으로 정한 값이면 **숫자를 같이** 남깁니다.
- 결정이 뒤집히면 **이전 항목을 지우지 말고** 새 항목에 "무엇을 뒤집었는지" 적습니다.
  이 프로젝트는 직관과 반대되는 결론이 많아서, 되돌린 이유가 기록에 남아야 또 되돌아가지 않습니다.
- 자세한 근거는 `docs/DECISIONS.md`(왜), `docs/BALANCE.md`(수치), `docs/RULES.md`(규칙),
  `docs/ARCHITECTURE.md`(구조), `docs/CONTENT.md`(에셋)로 나눠 적고, 여기서는 **링크만** 겁니다.

### 손대기 전에 알아야 할 것

1. `src/core/` 는 **DOM을 모릅니다.** 규칙은 전부 순수 함수라 브라우저 없이 테스트됩니다.
   의존 방향은 `ui → content → core` 한 방향입니다. 거꾸로 import 하지 마세요.
2. 규칙을 판정하는 곳은 **`src/core/rules.ts` 하나뿐**입니다. 조합 판정을 바꾸려면 여기만 고칩니다.
3. 밸런스 숫자는 **`src/content/stages.ts` 맨 위 두 줄**(EASIEST / HARDEST)이 전부입니다.
   중간 스테이지는 보간됩니다.
4. 스킨은 **`src/ui/styles/tokens.css`** 에서 시작합니다. 팔레트 값이 전부 여기 있습니다.
5. 바꾸고 나면:
   ```bash
   npm test          # 규칙 · 밸런스 · 튜토리얼 (81개)
   npm run build     # 타입 검사 + 번들
   npm run preview   # 그리고 다른 터미널에서
   CHROME_PATH=/opt/pw-browsers/chromium npm run test:layout   # 실제 브라우저 레이아웃
   ```
   레이아웃 깨짐은 **유닛 테스트로 절대 못 잡습니다.** 스킨을 건드렸으면 `test:layout` 을 꼭 돌리세요.

---

## 1. 지금 상태 한눈에

| 항목 | 값 |
| --- | --- |
| 이름 | TAP to TEN |
| 언어 | **영어**. 화면에 나오는 한글은 전부 뺐습니다 |
| 규칙 | 2~5칸을 골라 **합이 정확히 10**이면 사라짐. 위치 제약 없음, 같은 숫자 규칙 없음 |
| 점수 | 2칸 10 · 3칸 20 · 4칸 40 · 5칸 80 |
| 모드 | **타임어택(60초) · 무제한(생존)** 둘뿐. 스토리는 코드에 남아 있으나 화면에서 뺐습니다 |
| 스토리 | **99 스테이지** (11챕터 × 9). 판을 전부 지워 **뒤에 숨은 그림 얻기** |
| 수집 | 그림 99장 · 갤러리 화면. 별 등급은 없음 |
| 판 | **9×9, 81칸 고정.** 세 모드 전부 같습니다 |
| 표지 | `public/cover.webp` — 앱을 켜면 화면을 꽉 채웁니다 |
| 워드마크 | `public/cover-logo.svg` — 타이틀 화면 (벡터) |
| 스킨 | **밝은 종이 톤** — 크림 바탕, 로고의 주황·노랑, 마룬 글씨 |
| 소리 | WebAudio 합성 — 음원 파일 없음. 진동은 `navigator.vibrate`. 설정에서 따로 끔 |
| 흐름 | 타이틀 → (스토리: 챕터 → 스테이지 → 카드 / 시간 모드: 시작 화면) → 게임 |
| 패키징 | Capacitor 8 / Android targetSdk 36 / 세로 고정 |
| 테스트 | 유닛 99 + 브라우저 3벌 — 레이아웃(`test:layout`) · 드래그(`test:drag`) · 소리·진동(`test:feedback`) |

---

## 2. 타임라인

## 2026-08-25

### 13:20 · 빌드 도구 세팅 · [Claude]
Vite 7 + TypeScript + Vitest. 커밋 `1bded92`.

### 13:44 · 첫 게임 구현 · [Claude]
Woodber(Number Match) 계열 퍼즐로 출발. 커밋 `9af818d`.

### 13:51 · Android 패키징 · [Claude]
Capacitor로 감싸 Play 업로드 가능한 형태로. 세로 고정. 커밋 `97be70e`.

### 16:12 · 세 가지 모드 · [Claude]
스토리 / 타임어택 / 무제한. 커밋 `1dfa09e`.

### 17:16 · 단일 파일 빌드 · [Claude]
CSS·JS·SVG를 데이터 URI로 인라인해서 HTML 한 장으로 만드는 스크립트(`scripts/build-single-file.mjs`).
공유·미리보기용입니다. 커밋 `042b7c9`.

## 2026-08-26

### 16:19 · 규칙을 "합이 10"만 남기고 정리 · [Claude]
같은 숫자끼리 지우는 규칙 삭제. 이때 **보드가 아예 안 깨지는 사고**가 나서
`createBoard`(합10 조각을 이어붙여 배치)를 도입했습니다. → [DECISIONS 1·3](./DECISIONS.md)
커밋 `2f371c8`.

### 16:54 · 위치 제약 삭제 + 폴더 재구성 · [Claude]
"멀리 떨어진 칸도 같이 고를 수 있다"로 확정. `core / content / ui` 로 나누고 문서 폴더 신설.
커밋 `5b6a5a6`.

### 17:12 · 무제한을 생존 모드로 · [Claude]
블록이 계속 떨어지고 보드가 차면 끝. `minIntervalMs: 650` 이 이 모드의 전부입니다.
→ [BALANCE](./BALANCE.md) 커밋 `59c83d1`.

### 21:05 · 튜토리얼 + 내 기록 · [Claude]
직접 해보는 5단계 튜토리얼(정답이 하나뿐인 보드라 게임 규칙이 그대로 채점합니다) + 기록 화면.
커밋 `880f309`.

### 23:58 · 반응형 레이아웃 수정 · [Claude]
실제 아이폰에서 보드가 잘리고 양옆에 여백이 생기던 문제. 원인 두 가지:
HUD보다 먼저 보드를 재던 것, 프레임이 `.board-wrap` 에 붙어 있던 것.
`ResizeObserver` 추가하고 브라우저 레이아웃 테스트를 만들었습니다. 커밋 `38f51c0`.

## 2026-08-27

### 09:36 · TAP to TEN · 81칸 덱 · [Claude]
9×9에 1~9를 9개씩. 이름과 스킨을 브로드웨이 탭댄스 컨셉으로. 커밋 `f018fc0`.

### 15:50 · Monument Valley 스킨 시안 · [Claude] — **채택되지 않음**
파스텔 평면 스킨과, 보드를 6→11줄로 키우는 스토리 곡선을 만들었습니다.
같은 시각에 다른 갈래(아래 23:40~)가 진행되고 있었고, **디자인은 그쪽을 쓰기로 정했습니다.**
버려진 게 아까운 게 아니라 **측정치가 아까워서** 근거만 옮겨 왔습니다 →
[DECISIONS 11](./DECISIONS.md). 커밋 `87f93e4` (main 에는 안 들어감).

### 23:40 · 아르데코 극장 UI + 스토리 스테이지 성장 · [Codex]
스토리를 9×5 → 9×9 로 키우고, 남는 아래 공간을 캐릭터 무대로 잡았습니다.
스킨을 어두운 밤·황동의 극장 톤으로. 커밋 `1b07d18`.

### 23:42 · 보드 용량 안에서 배치되도록 수정 · [Codex]
커진 스테이지에서 딜이 보드 칸 수를 넘던 문제. 커밋 `47014d7`.

## 2026-08-28

### 00:03 · 캐릭터 무대 레이아웃 복구 · [Codex]
커밋 `241cbde`.

### 00:19 · 앱 흐름 정리 + 드래그 선택 · [Codex]
타이틀에 PLAY 를 두고 모드 목록을 접었습니다(`#mode-list.collapsed`).
`appStateMachine.ts` 로 화면 전환을 분리하고, 선택 합계 표시와 설정 저장을 추가했습니다.
커밋 `6564745`.

### 16:12 · 두 갈래 합치기 · [Claude]
main(Codex) 쪽 코드를 **전부 그대로 두고**, 제 갈래에서는 세 가지만 가져왔습니다.

1. **`docs/WORKLOG.md`** — 이 파일.
2. **[DECISIONS 11](./DECISIONS.md)** — 시뮬레이션 결과 네 가지. 특히 **11-4 는 확인이 필요합니다**:
   지금 곡선은 별 기준을 비율로만 잡아서 **★·★★ 목표 숫자가 뒤로 갈수록 커집니다**
   (★ 15 → 19). 봇으로 재보니 짝만 맞추는 플레이가 스테이지 1에서 별 1.2개,
   스테이지 10에서 2.2개를 받습니다. **첫 스테이지가 제일 빡빡합니다.**
   고치는 방법도 같이 적어뒀는데, 숫자는 사람이 조정 중이라 **손대지 않았습니다.**
3. **레이아웃 테스트 보강** — 화면마다 스테이지 1(9×5)과 **스테이지 20(9×9)** 을 둘 다 검사합니다.
   가장 큰 보드는 뒤쪽이라 앞만 봐서는 잘림을 놓칩니다. 그리고 타이틀의 PLAY 를
   먼저 눌러 모드 목록을 펴도록 고쳤습니다 — 안 그러면 클릭이 조용히 타임아웃합니다.

**디자인은 이제 사람이 직접 잡습니다.** 목업 이미지를 받아 CSS 로 옮기는 방식으로 진행합니다.

**남은 일**
- `public/story/*.svg` 캐릭터 4장은 아직 예전 나무 컨셉
- `#stage-floor` 는 비어 있음 — 캐릭터가 들어갈 자리
- DECISIONS 11-4 의 별 기준 역전 (확인 후 결정)

### 16:38 · 드래그·화면 흔들림·여백 세 가지 수정 · [Claude]

플레이해 보고 나온 것들입니다.

1. **드래그가 지나간 칸을 빠뜨렸습니다.** 빠르게 쓸면 이벤트가 한 번만 와서 첫 칸과 끝 칸만
   집혔고, 10을 넘길 때만 벌을 받으니 빠진 칸이 조용히 사라졌습니다. 결과적으로
   **빠를수록 쉬웠습니다.** 두 점 사이를 타일의 0.4배 간격으로 훑도록 고쳤습니다.
   → [DECISIONS 12](./DECISIONS.md)
2. **폰 브라우저에서 드래그하면 화면 전체가 출렁였습니다.** `body` 를 뷰포트에 못박고,
   보드 주변 요소에 `touch-action: none` 을 걸었습니다. → [DECISIONS 13](./DECISIONS.md)
3. **세 모드의 보드 여백이 달랐습니다** (스토리 21px, 나머지 16px). 원인 두 개:
   화면 등장 애니메이션의 `scale(.992)` 가 걸린 채로 보드를 재고 있었고
   (`getBoundingClientRect` 는 그려진 박스라서 transform 이 반영됩니다),
   보드 상자의 `aspect-ratio` 가 타일 간격과 안쪽 여백을 빼놓고 `가로칸/세로칸` 이었습니다.
   둘 다 고쳐서 **세 모드 모두 여백 16px · 타일 37px** 로 같아졌습니다.
   덤으로 타이머 막대가 스토리에서도 자리를 지켜서 보드 높이도 같아졌습니다.
   → [DECISIONS 9](./DECISIONS.md)

**새 테스트** — `tests/browser/drag.mjs` (`npm run test:drag`). 손 속도별 결과 일치,
드래그 중 페이지 스크롤 없음, 세 모드 보드 위치·크기 일치를 검사합니다.

### 17:50 · 스토리를 99 스테이지로, 목표는 "전부 지우기" · [Claude]

네 가지입니다.

1. **5칸을 골랐는데 10이 아니면 즉시 거절.** 여섯 번째가 없으니 그 선택은 이미 죽었는데
   화면에는 살아 있었습니다. 보드가 흔들리고 그 다섯 칸이 **빨갛게 번쩍인 뒤** 놓입니다.
   번쩍임이 핵심 — 그냥 사라지면 "안 눌린 건가"와 구분이 안 됩니다.
   → [DECISIONS 15](./DECISIONS.md)

2. **스토리 목표 = 판을 전부 지우기.** 완전 탐색으로 확인했습니다 — **모든 스테이지, 모든
   시드에서 판은 100% 전부 지울 수 있습니다.** 그런데 조각을 가로질러 지우면 짝 잃은
   타일이 남습니다. 봇 측정: 아무렇게나 두면 초반 35% → 후반 **0%**, 항상 긴 조합만
   집으면 거의 0%, **판을 계속 다 지울 수 있게 유지하는 수만 고르면 100%.**
   즉 **막히는 건 운이 아니라 실수**입니다. → [DECISIONS 14](./DECISIONS.md)

3. **한 수 물리기(undo) 추가.** 편의 기능이 아니라 위 목표를 공정하게 만드는 자원입니다.
   **판이 죽은 뒤에도 쓸 수 있고**, 그때 결과창이 "한 수 물리기"를 먼저 권합니다.
   마지막 스테이지에도 1회는 남겨둡니다 — 0이면 막힌 판을 살릴 방법이 사라집니다.

   별도 바뀌었습니다: ★★★ = 전부 지움 + 도움 0회 / ★★ = 전부 지움 / ★ = 조금 남김(진행은 됨).

4. **99 스테이지.** 11챕터 × 9. 보드 4줄→11줄, 배분 큰조각→짝위주, 힌트 5→0, 물리기 5→1.
   난이도 다이얼은 크기가 아니라 **실수의 대가**입니다. 스테이지별 측정치는 [BALANCE](./BALANCE.md).

**새 테스트** — `content/clearable.test.ts`. 모든 스테이지의 판이 정말 전부 지워지는지
완전 탐색으로 검사하고, 신중한 봇이 실제로 끝까지 비우는지도 확인합니다.
스토리의 전제가 깨지면 여기서 먼저 걸립니다.

**주의** — 챕터 11개가 캐릭터 그림 4장을 돌려 씁니다. 그림이 준비되면 `chapters.ts` 의
`character` 경로만 바꾸면 됩니다.

### 20:48 · 그림 수집 게임으로 · 숫자 비율 · 나누기 아이템 · [Claude]

구조를 바꿨습니다. 네 가지입니다.

1. **숫자 비율.** "4~5개 조합을 만들면 1과 2가 다 없어진다"가 측정으로 확인됐습니다 —
   1스테이지 판의 **1이 37%, 2가 18%로 전체의 55%**, 9는 1%였습니다. 원인은 딜러가
   그룹 크기만 정하고 숫자는 균등난수로 굴린 것. 이제 **합이 10인 그룹 목록에서
   "지금 판이 모자란 숫자"를 보고 고릅니다.** 1이 37%→23%, 9가 1%→8%.
   덤으로 이 비율이 그룹 크기까지 정해서 다이얼이 하나로 줄었습니다.
   → [DECISIONS 16](./DECISIONS.md)

2. **그림 드러내기.** 판 뒤에 그 스테이지의 그림이 깔리고, 지운 칸이 투명해지며
   그림이 드러납니다. 전부 지우면 **그림을 얻어 갤러리에 모읍니다.**
   따라오는 제약이 하나 — **스토리는 줄이 무너지면 안 됩니다**(`keepBoard`).
   한 줄이 비었다고 위가 내려앉으면 드러난 구멍이 그림과 어긋납니다.
   → [DECISIONS 17](./DECISIONS.md)

3. **별 등급 삭제.** 목표가 이분법이 됐으니(얻거나 못 얻거나) 별을 얹으면 상이 두 개가
   되어 흐려집니다. `내 기록` 화면은 **갤러리**가 됐습니다.

4. **나누기 아이템.** 블록 하나를 합이 같은 조각으로 쪼갭니다(5 → 3+2 또는 1+1+1+2).
   어떻게 쪼개질지는 못 고릅니다. **함정이 하나 있었습니다** — 한 수로 지울 수 있는 건
   5칸까지라, 4칸 그룹 안의 블록을 셋으로 쪼개면 6칸이 되어 못 지웁니다. 그래서
   **쪼갤 때마다 `canEmpty` 로 검사하고 판을 망치는 쪼개기는 다시 굴립니다.**
   → [DECISIONS 18](./DECISIONS.md)

**그림은 아직 임시입니다.** `content/gallery.ts` 가 스테이지 번호로 만든 단순한 도형을
씁니다. 진짜 일러스트가 오면 `art` 경로만 채우면 됩니다 → [CONTENT](./CONTENT.md).

**테스트** — 유닛 95개. `clearable.test.ts` 는 새 딜러에서도 모든 스테이지가 100%
비워지는지 계속 확인하고, `game.test.ts` 의 `splitTile` 은 40번 굴려서 **어떤 쪼개기도
판을 못 비우게 만들지 않는지** 검사합니다.

### 21:44 · 1탄을 쉽게 · 타이틀 레이아웃 · 블록 모양으로 드러나는 그림 · [Claude]

1. **1탄이 쉬워졌습니다.** 1스테이지를 **9×3(27칸)** 으로 줄였습니다. 아무렇게나 두는 봇 기준
   41% → **55%** 로 올랐고, 힌트 5·물리기 5·나누기 3까지 있으니 사실상 실패하지 않습니다.
   **숫자 비율을 건드려서 쉽게 만드는 안은 버렸습니다** — 성공률은 오르지만 1·2 쏠림이
   그대로 돌아옵니다(1+2가 47%→58%). 판을 한 줄 줄이면 비율은 그대로 두고 성공률만 오릅니다.
   → [DECISIONS 20](./DECISIONS.md)

2. **타이틀 레이아웃.** 가운데 정렬 하나로 다 몰아놨더니 PLAY 아래위로 빈 구멍이 두 개
   생겼습니다(접힌 모드 목록에도 `gap` 이 그대로 붙어서). 이제 **위: 워드마크 + 모은 그림,
   아래: PLAY + 링크 한 줄** 두 덩어리입니다. 링크 4개는 한 줄에 들어가도록 작게 잡았고,
   **가운데 남는 공간이 캐릭터 아트 자리**입니다. → [CONTENT](./CONTENT.md)

3. **그림이 블록 모양으로 드러납니다.** 판 전체에 배경을 깔고 타일을 투명하게 하면 드러난
   부분이 간격까지 이어진 **한 덩어리**가 됩니다. 이제 **지운 칸마다 그 칸의 조각**을
   배경으로 넣어서, 모서리가 둥근 블록 그대로 올라옵니다. **갤러리에 들어가는 건 나뉘지
   않은 원본**입니다 — 나누는 건 판 위 연출뿐. → [DECISIONS 19](./DECISIONS.md)

### 18:50 · 로고를 그리고, 거기서 스킨을 뽑고, 판을 9×9로 · [Claude]

1. **워드마크를 블록으로 그렸습니다.** 폰트가 아니라 그림입니다 — 게임이 블록으로 되어
   있으니 로고도 그래야 하고, 그건 어떤 폰트로도 안 됩니다. `scripts/build-logo.mjs` 가
   글자 비트맵에서 `public/logo.svg` 를 만듭니다. 블록 하나는 세 겹(뒤로 밀린 어두운 판,
   그라데이션 면, 위쪽 밝은 띠)이고 **게임판 블록도 같은 구성**으로 맞췄습니다.

2. **팔레트를 로고에서 뽑았습니다.** 차가운 쪽(#5ec9f5→#3d5be0), 뜨거운 쪽(#ff5fd2→#8b3fe0),
   그리고 찾아야 하는 것에만 쓰는 주황(#ffc635→#ff8a1f). **색이 의미를 갖습니다** —
   도구 버튼이 전부 금색이던 때는 어느 게 뭔지 알 수 없었는데, 이제 주황=힌트,
   파랑=나누기, 분홍=되돌리기입니다. → [DECISIONS 22](./DECISIONS.md)

3. **판을 9×9로 고정했습니다.** 스토리·타임어택·무제한 전부 81칸입니다.
   **대가가 있습니다** — 81칸은 계속 지울 수 있게 유지하기 어려워서, 아무 수나 두는 봇의
   완전 클리어율이 27칸 시절 53%에서 **0%** 로 떨어집니다(신중한 봇은 여전히 100%).
   판 크기가 다이얼에서 빠졌으니 **초반의 관대함은 되돌리기가 대신합니다**(1스테이지 8회).
   숫자 배분으로 대신하는 안은 16번에서 고친 1·2 쏠림을 되돌리므로 안 씁니다.
   → [DECISIONS 21](./DECISIONS.md), [BALANCE](./BALANCE.md)

4. **무제한 레이아웃을 나머지와 맞췄습니다.** 판이 9×9로 같아졌고, 도구가 없는 모드도
   **버튼 자리는 남깁니다**(`visibility: hidden`). 힌트 버튼이 세 모드에서 같은 자리에 옵니다.


### 19:02 · PLAY 버튼 삭제 · 타이틀 재배치 · 그림 완성 연출 · [Claude]

1. **PLAY 버튼을 없앴습니다.** 세 모드가 처음부터 나와 있습니다. 버튼 하나가 게임을 한 번
   더 가렸을 뿐이고, 로고가 커지면서 **그 버튼이 펼치는 목록이 화면 밖으로 잘렸습니다.**
   접히는 목록을 없애니 잘림도 같이 사라졌습니다.

2. **타이틀을 남는 공간을 나눠 갖는 한 열로** 바꿨습니다. 화면이 짧으면 모은 그림 줄이
   먼저 접히고(700px 이하) 더 짧으면 통째로 빠집니다(580px 이하). 로고도 같이 작아집니다.
   852·667·560 세 크기에서 링크까지 전부 화면 안에 들어옵니다.

3. **판을 다 지우면 완성된 그림이 2초 머뭅니다.** 조각으로 드러나 있던 그림 위로
   **나뉘지 않은 원본**이 겹쳐 올라오면서 블록이 그림으로 정리되고, 그 다음에 결과창이
   뜹니다. 예전엔 마지막 블록을 지운 순간 결과창이 같이 떠서, 스테이지 내내 목표였던
   그림을 정작 못 보고 넘어갔습니다. → [DECISIONS 23](./DECISIONS.md)

4. 모드 버튼도 블록처럼 보이게 고쳤습니다(검은 알약 → 파란 면 + 아래 슬랩).

### 19:22 · 목업 기반 개편 1차 — 색 블록 · HUD · 시간 기록 · [Claude]

20화면 목업을 받아 **구조부터** 맞추기 시작했습니다. 이번 차수는 게임 화면입니다.

1. **블록마다 색이 생겼습니다.** 숫자 1~9에 각자의 색(`--v1`~`--v9`). 흰 블록만 있던
   판은 숫자를 하나씩 읽어야 했는데, 색이 붙으면 **덩어리로 읽힙니다.** SUM 줄의 항도
   같은 색을 씁니다.

2. **HUD를 목업대로.** 가운데 `STAGE 10`, 그 아래 **REVEAL · TIME · COMBO** 세 칸.
   세 칸은 모드가 달라도 같은 자리이고 라벨만 바뀝니다(타임어택은 TIME·SCORE·COMBO,
   무제한은 TIME·SCORE·BEST). 점수 중심이던 헤더가 **그림을 얼마나 드러냈는지** 중심으로
   바뀌었습니다.

3. **SUM 이 식이 됐습니다.** `SUM 2 + 3 = 5` 처럼 고른 블록이 그대로 식으로 나오고,
   10이 되면 주황으로 바뀝니다. 판 아래 한 줄을 통째로 씁니다.

4. **모든 모드가 시계를 돕니다**(`GameState.elapsedMs`). 스토리에 **스테이지별 최고
   기록**이 생겼고(`Progress.bestTimes`), 클리어 화면에 `REVEAL 100% · TIME 00:48 ·
   새 최고 기록!` 이 나옵니다. 실패 화면은 `GAME OVER` 로 바뀌었습니다.

5. **패널 버튼도 블록으로.** 초록=계속, 파랑=선택, 빨강=다시. 금색 버튼 하나로 다 하던 걸
   목업의 색 규칙에 맞췄습니다.

**다음 차수** — 챕터 선택 → 스테이지 선택(20칸 그리드) → 스테이지 정보 모달, 타임어택·
무제한 시작 화면, 튜토리얼 5단계 화면. **상점·업적·일일미션·하트/코인/보석은 별도 논의**
가 필요합니다(과금 구조라 게임 설계와 별개 결정).

### 22:11 · 목업 기반 개편 2차 — 판 크기 · 갤러리 이동 · 고르는 화면 셋 · [Claude]

1. **판이 다시 커졌습니다.** `.board-wrap` 이 `aspect-ratio` 를 가진 채로 세로 공간을
   양보하고 있어서, HUD 가 늘어난 만큼 **가로까지 같이 줄어들고 있었습니다**(화면의 68%).
   비율 상자를 걷어내고 남는 공간을 그대로 쓰게 하니 **92%** 로 돌아왔습니다.
   `boardView.ts` 의 `--board-ratio` 계산과 비어 있던 `#stage-floor` 도 함께 삭제했습니다.

2. **결과창의 "갤러리 보기"가 갤러리로 갑니다.** `result → gallery` 가 허용되지 않은
   이동이라 예외로 막히고, 끝난 게임 화면에 그대로 남아 있었습니다. 전환을 열고
   회귀 테스트를 붙였습니다.

3. **고르는 화면 셋을 새로 만들었습니다.**

   ```
   타이틀 ─ 스토리 ──→ 챕터 목록 ─→ 스테이지 그리드 ─→ 스테이지 카드 ─→ 게임
          ├ 타임어택 ─→ 시작 화면 ────────────────────────────────→ 게임
          └ 무제한 ──→ 시작 화면 ────────────────────────────────→ 게임
   ```

   - **챕터 목록** (`#screen-chapters`) — 11개 챕터, 오른쪽에 `모은 그림 / 9`.
     다 모은 챕터는 주황으로 켜지고, 아직 못 여는 챕터는 `잠김`
   - **스테이지 그리드** (`#screen-stages`) — 한 챕터의 9칸을 3×3 으로. 9칸은 폰을
     못 채우니 가운데에 놓았습니다(짧은 화면에서는 위가 잘리지 않도록 `auto` 여백)
   - **스테이지 카드** — 걸려 있는 그림(못 얻었으면 `?`)과 `BEST TIME`. 바깥을 눌러도
     닫힙니다
   - **시작 화면** (`#screen-intro`) — 타임어택은 `60`, 무제한은 `∞`. 무제한은
     **버틴 시간 최고 기록**도 같이 남기기 시작했습니다(`Progress.bestEndlessMs`)

4. **게임 안 뒤로가기는 들어온 길로 나갑니다.** 스토리는 스테이지 그리드로, 시간 모드는
   타이틀로. 결과창의 "다시 하기"는 고르는 화면을 거치지 않고 그 자리에서 다시 시작합니다.

5. 브라우저 테스트 두 벌도 새 흐름을 걷도록 고쳤습니다(`startRun` / `backToTitle`).
   유닛 99개 · 레이아웃 24개 · 드래그 9개 전부 통과.

**남은 것** — 튜토리얼 5단계 화면 손보기, 갤러리 상세(설명 + ←/→), 설정 화면.
상점·업적·일일미션·하트/코인/보석은 **빼기로 했습니다**(과금 구조 없음).

## 2026-08-29

### 10:59 · 로고 교체 · 효과음과 진동 · 설정 화면 · 갤러리 상세 · [Claude]

1. **로고를 받은 아트워크로 바꿨습니다.** 스플래시(글자였음)와 타이틀이 이제 **같은 파일**
   (`public/logo.webp`)을 씁니다 — 첫 화면이 타이틀로 이어지지 끊기지 않습니다.
   1.9MB PNG 를 투명 여백까지 잘라내고 900px WebP 로 다시 인코딩해 **133KB**.
   비트맵에서 SVG 를 만들던 `scripts/build-logo.mjs` 와 `public/logo.svg` 는 지웠습니다 —
   그려 온 로고가 스크립트가 흉내 내던 것보다 나은데 그걸 코드로 재현할 이유가 없습니다.

2. **효과음이 생겼습니다. 파일은 하나도 없습니다.** 전부 WebAudio 로 그 자리에서
   합성합니다(`src/ui/feedback.ts`). 퍼즐 게임이 내는 소리는 짧은 음 몇 개뿐이고,
   그건 **받는 것보다 설명하는 게 싸기** 때문입니다 — 용량 0, 디코딩 0, 연결이 나빠도
   늦게 오지 않습니다.

   - **고를 때** — 5음 음계를 올라갑니다. 많이 고를수록 높아져서, 쓸어 담는 동안
     뭔가 쌓이는 소리가 납니다
   - **지울 때** — 3화음. 연속으로 지우면 한 칸씩 위로 올라갑니다. 4개 이상 조합은
     음이 하나 더 붙습니다
   - **틀렸을 때** — 내려가는 사각파. 올라가는 소리와 절대 헷갈리지 않습니다
   - **완성 / 실패** — 각각 올라가는 아르페지오와 내려가는 3음

3. **진동도 됩니다** (`navigator.vibrate`, 안드로이드 WebView 에서 그대로 동작).
   고를 때 8ms, 지울 때 14ms, **틀렸을 때는 `30-60-30` 두 번 두드림**입니다 —
   단발로 하면 성공과 구분이 안 됩니다.

4. **설정이 화면이 됐습니다** (`#screen-settings`). 예전엔 결과 오버레이 한 줄이라,
   소리를 끄려면 다른 용도로 만든 패널을 열어야 했습니다. 스위치 둘 다 **기본 켜짐**.
   진동을 지원하지 않는 기기에서는 그렇다고 적어 줍니다 — 켰는데 아무 일도 없으면
   고장 난 걸로 보이니까요.

5. **갤러리 상세에 ←/→ 가 생겼습니다.** 모은 그림 사이를 목록으로 돌아가지 않고 넘깁니다.
   그림 아래에 챕터와 **그때 걸린 시간**이 붙습니다.

6. **소리·진동 테스트**를 새로 만들었습니다(`npm run test:feedback`). 소리는 **조용히
   고장 나서** — 안 나는 소리와 없는 소리는 구분이 안 됩니다 — `createOscillator` 와
   `navigator.vibrate` 를 감싸 횟수를 셉니다. 채널을 하나만 꺼도 다른 하나는 남는지,
   거절 진동이 성공과 다른 모양인지까지 봅니다.

유닛 99 · 레이아웃 24 · 드래그 9 · 피드백 9 전부 통과.

**남은 것** — 튜토리얼 5단계 화면 손보기.

### 11:48 · 챕터 이름을 번호만 남김 · [Claude]

챕터 제목에서 소제목을 뗐습니다 — `1장 · 첫 그루터기` → **`1장`**. 열한 개 전부.
이야기가 아직 정해지지 않았는데 이름만 먼저 붙어 있었고, 챕터 선택 화면에서는 그 이름이
오른쪽 진행 숫자를 밀어내고 있었습니다.

나중에 이야기가 정해지면 `chapters.ts` 의 `title` 에 다시 붙이면 됩니다 — 화면 쪽은
`title` 을 그대로 찍기만 하므로 고칠 곳이 없습니다.

챕터 이름과 그림 이름을 이어 붙이던 `PickerScreen.titleOf` 도 지웠습니다. 쓰이지 않는
데다, 방금 없앤 바로 그 형식을 만들고 있었습니다.

## 2026-08-30

### 13:25 · 표지와 워드마크 교체 · [Claude]

받은 그림 두 장을 넣었습니다. 그 외에는 아무것도 안 건드렸습니다.

1. **첫 화면이 표지 일러스트가 됐습니다** (`public/cover.webp`). 화면을 꽉 채우고, 그 위에
   아무것도 그리지 않습니다 — 워드마크도 캐릭터도 그림 안에 이미 있으니까요. 전면
   일러스트라 다른 화면이 지키는 세이프에어리어 여백을 혼자 무시합니다.
   원본 4168×9331 PNG(5.5MB)를 **가로 1080 WebP(250KB)** 로 줄였습니다.

2. **타이틀 워드마크를 SVG 로 바꿨습니다** (`public/cover-logo.svg`, 54KB). 벡터라 화면
   배율을 신경 쓸 일이 없습니다. 새 그림은 **세로가 더 긴** 비율(618×726)이라 폭을
   320 → **190px** 로 줄였습니다 — 예전 가로형 마크와 같은 높이를 유지하는 값입니다.
   375·393·430 세 폭에서 아래 링크까지 안 잘리는 것 확인했습니다.

3. 죽은 것 정리 — `public/logo.webp`, `.splash-mark` / `.splash-pulse` 규칙,
   `splash-rise` / `splash-breathe` 키프레임.

**알아둘 것** — `tokens.css` 의 팔레트(시안·마젠타)는 **예전 블록 로고에서 뽑은 색**이라
지금 워드마크(주황·노랑)와 안 맞습니다. 스킨 전체를 새 로고에 맞추는 건 아직 안 했습니다.

원본 `cover.png` 는 저장소 루트에 그대로 뒀습니다. `public/` 밖이라 빌드에는 안 들어갑니다.

유닛 99 · 레이아웃 24 · 드래그 9 · 피드백 9 전부 통과.

### 13:45 · 표지 잘림 해결 · 타이틀에서 그림 줄 제거 · [Claude]

1. **표지가 이제 안 잘립니다.** `cover` → **`contain`**. 폰은 한 가지 모양이 아니라서
   (13 mini 0.462 · 15 0.461 · SE 0.562) 잘라 맞추면 기기마다 다른 부분이 날아가고,
   세로 표지에서는 그게 **위아래** — 로고와 스튜디오 마크가 있는 자리입니다.

   보통 레터박스는 검은 띠를 대가로 치르는데 여기서는 아닙니다. **그림 바탕이 흰색이고
   화면 바탕도 흰색**이라 띠가 그림 가장자리와 같은 색입니다. 안 잘리고, 티도 안 납니다.

2. **타이틀에서 모은 그림 줄을 없앴습니다.** 워드마크와 같은 자리를 두고 다퉜고,
   갤러리가 더 잘 하는 말을 대신 하고 있었습니다. 그 자리는 워드마크가 가져갔습니다 —
   폭 190 → **250px**. 13 mini에서 로고 250×293, 아래 링크까지 다 들어갑니다.

유닛 99 · 레이아웃 24 · 드래그 9 · 피드백 9 전부 통과.

**다음** — 새 표지 이미지 교체(파일 받는 대로), 새 로고 색에 맞춘 스킨 전면 수정.

### 14:05 · 새 표지 · 밝은 스킨 · 타임어택 보상 · [Claude]

1. **표지를 `cover2` 로 교체**했습니다 (4837×9540 → 1080×2130 WebP, 253KB).
   비율이 0.447 → **0.507** 로 덜 길어져서 `contain` 여백도 함께 줄었습니다.
   예전 `cover.png` 는 지웠습니다.

2. **밝은 스킨으로 전면 교체.** 표지 때문만이 아닙니다 — 블록을 지우면 **밑의 그림이
   드러나는데** 그 그림은 흰 배경 토이 렌더입니다. 남색 판에서는 지울 때마다 **흰 구멍이
   뻥 뚫렸습니다.** 종이 위에서는 그게 자연스럽게 이어집니다.

   - 바탕 크림 `#fff6e9`, 글씨 마룬 `#3a0f0a`, 강조는 로고의 주황·노랑
   - 블록 9색은 색상환에 **고르게 퍼뜨렸습니다.** 판은 덩어리로 읽히므로 35px 에서
     헷갈리는 두 색이 있으면 안 됩니다. 8번은 처음에 청록이었는데 1·2번과 함께 파랑이
     셋이 되어서 **초콜릿색**으로 바꿨습니다 — 종이가 감당하는 유일한 따뜻한 어두운 색
   - 화면별 파일에 흩어져 있던 rgba 81개를 `--panel` `--slab` `--recess` `--sheen`
     `--scrim` 토큰으로 정리했습니다. 다음 스킨 변경은 훨씬 쌉니다

3. **타임어택에 보상이 생겼습니다.** 예전엔 잘 지울수록 타일만 빨리 떨어졌습니다.

   - **100점마다 이미 지워진 빈칸 9개를 작은 블록으로 되채웁니다** (1·2·3 중 랜덤).
     긴 조합이 늘 모자란 게 작은 수라 이건 잡동사니가 아니라 연료입니다
   - **500점에 +30초와 새 판** (한 판 60 → 90초). 한 번만 — 두 번 주면 무제한 모드가
     됩니다
   - 둘 다 점수에서 읽어냅니다. 타임어택엔 물리기가 없어서 점수가 되돌아가지 않으니
     별도 상태가 필요 없습니다
   - 시계가 안 줄거나 판 밑에 줄이 생기는 걸 시간에 쫓기는 사람이 알아채긴 어려워서
     HUD 에 알림을 띄웁니다

유닛 104 · 레이아웃 24 · 드래그 9 · 피드백 9 전부 통과.

### 18:30 · 스토리 모드 화면에서 제거 · 전체 영어화 · 새 표지 · GREAT! 연출 · [Claude]

1. **스토리를 타이틀에서 뺐습니다.** 타임어택과 무제한 둘만 남습니다. 챕터·스테이지·
   그림 코드는 **지우지 않고 남겨** 뒀습니다 — 되살리려면 버튼 하나만 다시 넣으면 됩니다.
   갤러리는 그림을 채울 길이 없어졌지만 두 모드의 최고 기록은 여전히 쓸모가 있어서
   **RECORDS** 로 이름만 바꿔 남겼습니다.

2. **화면의 한글을 전부 영어로.** 211줄 — 버튼·안내문·규칙·튜토리얼·스토리 대사,
   그리고 브라우저 테스트 출력까지. 어려운 말은 안 썼습니다
   (`Clear every block to win the picture`, `Make ten to score`).

3. **표지를 `cover-3` 로 교체**하고 **가로폭을 항상 채우도록** 바꿨습니다.
   `contain` 은 세로가 긴 폰에서 그림을 작게 만들었는데, 위아래 흰 여백이 있으니
   **가로를 채우고 넘치는 여백만 잘라내는 게** 맞습니다. `margin:auto` 대신 절대 배치 +
   `translateY(-50%)` — 그림이 화면보다 크면 auto 여백은 위로 붙어 버려서, 짧은 폰에서
   아래만 잘렸습니다.

4. **판이 끝나면 GREAT! 이 뜹니다** (`src/ui/screens/cheer.ts`). 점수에 따라
   `GOOD TRY! / NICE! / GREAT! / AMAZING!`. 1.4초 뒤 결과창이 뜹니다 — 예전엔 마지막
   수와 숫자 패널이 같은 순간에 와서 "끝냈다"가 아니라 "채점당했다"로 읽혔습니다.
   **애니메이션 파일은 아직 없어도 됩니다** — `CHEER_ART` 가 `null` 이면 글씨만 나옵니다.

유닛 104 · 레이아웃 24 · 드래그 9 · 피드백 9 전부 통과.

### 18:52 · 타임어택 판 고정 · GREAT! 연출에 영상 · 규칙 버튼 정리 · RECORDS 제거 · [Claude]

1. **타임어택 판이 이제 안 줄어듭니다** (`keepBoard: true`). 한 줄을 다 지우면 그 줄이
   접혀서 81 → 72 → 63 → 54 로 판이 작아지고 있었습니다. 판은 고정된 9×9 틀이고
   지운 칸은 구멍으로 남습니다 — 100점 보상이 되채울 자리도 그 구멍입니다.

2. **연출이 화면 전체를 어둡게 깔고 그 위에 나옵니다** (검정 50%). 글씨 뒤에 **춤추는
   영상**이 재생되고, **길이는 영상이 정합니다**(`ended` 이벤트). 영상은 여러 개를 넣어
   두면 **매번 무작위로** 하나를 고릅니다.

   - 목록이 비면 글씨만 1.4초 — 영상이 없어도 게임은 멀쩡합니다
   - 재생이 실패하거나 8초를 넘기면 자동으로 결과창으로 넘어갑니다. 어떤 경우에도
     어두운 화면에 갇히지 않습니다
   - 글씨 크기를 줄였습니다. `AMAZING!` 과 `GOOD TRY!` 가 양옆으로 삐져나가고 있었습니다

3. **규칙 패널은 버튼 하나** — `Close` 만. 규칙은 하던 것 위에 얹히는 것이라 닫으면
   그대로 돌아갑니다. 다른 데로 갈 이유가 없습니다. `Overlay` 에 `secondary: null` 을
   넣어 "버튼 하나짜리 패널"을 명시할 수 있게 했습니다 — 예전엔 생략하면 기본값
   `Menu` 가 붙었습니다.

4. **RECORDS 를 타이틀에서 뺐습니다.** 그림을 모으지 않으니 채울 게 없습니다.

유닛 105 · 레이아웃 24 · 드래그 9 · 피드백 9 전부 통과.

### 20:05 · 규칙 버튼 진짜 삭제 · 표지 04 · 춤추는 영상 + 소리 · [Claude]

1. **CI 가 지난 커밋부터 빨간불이었습니다 — 제 실수입니다.** `keepBoard` 로 바꾸면서
   `autoRefill` 을 지웠는데 `content.test.ts` 가 그 플래그를 아직 검사하고 있었습니다.
   로컬에서 `npm test | tail -3` 으로 확인하는 바람에 **합격/불합격 줄이 잘려 나가** 못
   봤습니다. 이제 `grep "Tests "` 로 확인합니다. `autoRefill` 은 읽는 데가 없는 죽은
   플래그라 타입에서도 뺐습니다.

2. **규칙의 Menu 버튼이 진짜로 사라졌습니다.** 지난번에 `secondary: null` 을 넣었는데
   **`.hidden` 규칙이 없어서** 클래스만 붙고 그대로 보였습니다. 화면별로 `.screen.hidden`
   `.overlay.hidden` 처럼 다 따로 정의돼 있었거든요. `tokens.css` 에 **범용 `.hidden`** 을
   넣었습니다. 확인 테스트도 클래스 이름이 아니라 **실제 높이**를 재도록 고쳤습니다 —
   그게 지난번에 통과로 잘못 나온 이유입니다.

3. **표지 `cover-04`** 로 교체 (1080×2130 WebP, 441KB).

4. **춤추는 영상이 나옵니다** (`public/movie/3.webm` + `3.mp3`).
   - **그림은 음소거, 소리는 옆에서 따로.** 소리가 든 영상은 브라우저가 자동재생을 거부할
     수 있어서 갈라 놨습니다. 둘은 같은 순간에 시작하고 길이가 같습니다
   - 소리는 설정의 **효과음 스위치**를 따릅니다
   - **글씨는 영상 위로 올렸습니다.** 한 칸을 같이 쓰다 보니 캐릭터 얼굴을 가리고 있었어요.
     이제 두 줄이고, 둘을 묶어 화면 가운데에 놓습니다
   - 지금 영상이 **4.7초**라 결과창까지 그만큼 걸립니다. 길면 잘라 주세요

유닛 107 · 레이아웃 14 · 드래그 · 피드백 전부 통과.

### 20:35 · 연출이 마지막 장면에서 멈추고 탭을 기다립니다 · [Claude]

**영상 파일은 멀쩡합니다.** 모서리 픽셀 알파가 0 — 진짜 투명이고, 크로미움에서 판이
캐릭터 팔 사이로 비쳐 보이는 것까지 확인했습니다. **아이폰에서 까맣게 보인다면 iOS
사파리가 VP9 알파를 못 읽어서**입니다. 아이폰에서도 투명하려면 같은 영상을 HEVC 알파로
한 번 더 뽑아 `<source>` 두 줄로 넣어야 합니다.

**연출이 시간이 아니라 플레이어로 끝납니다.** 영상이 끝나면 **마지막 장면에서 멈추고**
`TAP TO CONTINUE` 가 깜빡입니다. 누르면 결과창으로 갑니다.

- **재생 중에는 탭이 통하지 않습니다.** 손가락이 스쳐서 연출을 건너뛰는 일이 없도록,
  다 끝난 뒤에야 화면이 탭을 받습니다
- 영상이 아예 안 뜨거나 15초를 넘기면 그때도 탭 안내가 나옵니다 — 어두운 화면에 갇히지
  않습니다
- 60초를 실제로 흘려보내며 끝까지 확인했습니다: 연출 뜸 → 재생 중 hold 아님 → 끝나고
  `4.73/4.73` 에서 멈춤 + 안내 표시 → 탭 → 결과창

유닛 107 · 레이아웃 · 드래그 · 피드백 전부 통과.

### 20:22 · 연출 레이아웃 고정 · [Claude]

**글씨 · 영상 · 안내문이 나타날 때마다 나머지가 밀리고 있었습니다.** 세 줄이 내용에 맞춰
크기를 잡다 보니, 글씨가 뜨고 → 영상이 나타나 글씨를 밀고 → 안내문이 나타나 또 밀었습니다.

이제 **세 칸 모두 처음부터 자리를 잡아 둡니다** — `1.2em` / `46vh` / `1.6em`. 안내문은
`display` 가 아니라 `visibility` 로 나타나고, 영상 칸은 높이가 고정이라 비율이 다른
영상으로 바꿔도 자리가 안 변합니다.

실제 판을 끝까지 돌려 네 시점에서 좌표를 쟀습니다. 전부 동일합니다.

```
appears      word [43,182] clip [28,227] tap [111,644] hidden
clip playing word [43,182] clip [28,227] tap [111,644] hidden
mid clip     word [43,182] clip [28,227] tap [111,644] hidden
held (tap)   word [43,182] clip [28,227] tap [111,644] visible
```

유닛 107 · 레이아웃 · 드래그 · 피드백 전부 통과.

### 23:35 · 아이폰 레이아웃 · 점수 카드 4초 · 작은 글씨 확대 · [Claude]

1. **아이폰에서 춤추는 캐릭터가 아래로 내려간 원인 — `vh`.** 연출 칸을 `46vh` 로 잡고
   있었는데, iOS 의 `vh` 는 **브라우저 바가 걷힌 최대 높이**를 가리킵니다. 실제 보이는
   높이보다 커서 칸이 상자 밖으로 넘쳤고, 그만큼 아래로 밀렸습니다. 이제 **상자의
   퍼센트**(`52%`)로 잡습니다 — `.cheer` 는 `inset: 0` 이라 언제나 실제 가용 높이입니다.
   (실기기 확인은 못 했습니다. 원인으로 가장 유력한 것을 고쳤습니다.)

2. **연출 격자에 숨은 두 번째 열이 있었습니다.** 점수 카드가 세 줄을 다 차지하니
   자동 배치가 나머지를 **새 열**로 밀어냈습니다 — `34.5px 322.5px`. 그래서 카드가 왼쪽
   34px 조각에 갇혀 글씨가 줄바꿈됐습니다. 넷 다 `grid-column: 1` 로 못박았습니다.

3. **판이 끝나면 두 박자로 끝납니다.** `TIME OUT` / `BOARD FULL` 과 **큰 점수**를 4초 →
   글씨와 춤추는 영상 → 탭 → 결과창.

4. **작은 글씨를 키웠습니다.** `SUM` 9 → 13px, 합계 21 → 26px, HUD 라벨 10 → 13px,
   안내문 12 → 14px. 대신 **가로 화면에서 타일이 18 → 16px** 로 떨어져서, 짧은 화면
   구간(`max-height` 660 / 520)에서 그만큼 다시 조입니다. 18px 복구 확인.

유닛 107 · 레이아웃 · 드래그 · 피드백 전부 통과.

### 23:52 · 아이폰에서 화면 아래가 잘리던 진짜 원인 · [Claude]

지난번에 연출 칸의 `vh` 를 고쳤는데 **그건 증상의 일부였습니다.** 스크린샷을 다시 보니
영상만이 아니라 **판 아래쪽과 SUM 줄까지 통째로 브라우저 바 뒤에 가려져** 있었습니다.
앱 전체가 화면보다 컸던 겁니다.

**원인** — `body { position: fixed; inset: 0 }`. `inset: 0` 은 초기 컨테이닝 블록을 재는데,
iOS 사파리에서 그건 **주소창이 걷힌 상태의 최대 높이**입니다. 그래서 앱이 실제 보이는
영역보다 크고, 가운데 정렬된 것들은 그 차이의 절반만큼 아래로 밀립니다.

**고친 방법** — 두 겹입니다.

1. `height: 100dvh` — 지금 이 순간의 높이. iOS 15.4 이상
2. `--app-h` — `visualViewport.height` 를 재서 넣습니다(`src/ui/viewport.ts`).
   그보다 낮은 버전과, 앞으로 또 다르게 재는 경우까지 덮습니다

판은 `resize` 에서 다시 재는데 **비주얼 뷰포트만 바뀔 땐 그 이벤트가 안 옵니다.**
높이가 바뀌면 `resize` 를 직접 쏴 줍니다.

**실제로 재현해서 확인했습니다.** 레이아웃 뷰포트 852 안에서 보이는 높이만 700 으로
줄여 놓고:

```
full height  {"appH":852,"tile":37,"clipped":false,"sumVisible":true}
bar showing  {"appH":700,"tile":37,"clipped":false,"sumVisible":true}
```

앱이 700 을 따라가고, 판도 SUM 줄도 화면 안에 있습니다. 고치기 전이라면 앱은 852 그대로였습니다.

**영상이 흰 배경인 건 별개**입니다 — iOS 사파리가 VP9 알파를 못 읽는 문제 그대로입니다.

유닛 107 · 레이아웃 · 드래그 · 피드백 전부 통과.

## 아이폰: 춤 영상이 아래로 내려오고, 소리가 안 나던 것

**글씨가 자기 칸을 넘쳤습니다.** `.cheer-word` 의 칸 높이는 `1.2em` — 이 요소 기준으로
19px 남짓인데, 글씨 자체는 `clamp(30px, 11vw, 68px)`, 43px 쯤입니다. 넘친 만큼 아래
영상 칸을 밀고 들어갔고, 그래서 춤추는 애가 내려와 "탭" 안내와 겹쳤습니다.

칸 높이를 미리 정하지 않는 쪽으로 바꿨습니다 — `auto minmax(0, 1fr) auto`. 글씨와 안내는
필요한 만큼 가져가고 영상이 남은 자리를 다 씁니다. 넘칠 수가 없습니다. 넷 다
`grid-column: 1` 을 달아 둔 건 그대로입니다.

**박스만 재는 검사로는 안 잡혔습니다.** 박스는 안 겹치고 *글씨만* 넘쳤기 때문입니다.
`Range.getBoundingClientRect()` 로 잉크를 재고 `scrollHeight - clientHeight` 로 넘침을
같이 보게 고쳤습니다. 여섯 가지 화면 크기 전부 깨끗합니다.

**소리는 아이폰이 막고 있던 겁니다.** 노래는 판이 끝나고 4초 뒤 타이머에서 시작하는데,
아이폰은 터치 안에서 한 번도 재생된 적 없는 `<audio>` 를 타이머에서 트는 걸 거부합니다.
첫 터치 때 4밀리초짜리 무음 wav 를 틀었다 멈추는 걸로 풀었습니다 — `Feedback.unlock` 이
오디오 컨텍스트에 쓰던 것과 같은 수법이고, 같은 자리에서 같이 부릅니다.

되감기도 손봤습니다. `src` 를 새로 넣으면 알아서 0 부터고, 같은 파일을 다시 틀 때만
`currentTime = 0` 이 필요합니다. 둘을 한꺼번에 하면 사파리가 아직 헤더도 안 읽은 파일을
seek 한다고 던집니다. `start()` 하나로 갈랐습니다.

**옆면 무음 스위치는 별개**입니다 — 그건 코드로 못 이깁니다. 아이폰이 무음이면 확인해
보세요.

유닛 107 · 레이아웃 · 드래그 · 피드백 전부 통과.
